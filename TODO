bool\uint\address\bytes\string
mapping(keyType => valueType)

public\private\internal\external
pure\view

error（require、revert、assert）;自定义抛出error信息;

constant //常量，在编译时赋值（在写程序的时候）
constructor
immutable //在部署时，可在构造函数中赋值，可获取部署时的全局变量如msg.sender ;string和bytes不能为immutable

modifier
event
struct
interface

is
virtual
override

payable
receive
fallback
transfer
send
call
balance

delegatecall
salt
try / catch
library

keccak256(abi.encodePacked(,,,)) //hash运算
abi.encode(x)
abi.encodeWithSignature("methName(para)",v); //调用合约函数

selfdestruct(_address)//删除合约

selector
ERC20 //9个函数、2个事件
ERC721 //16函数、3个事件
ERC1155 //10个函数、4个事件

-javaagent:C:\Users\Administrator\Downloads\jihuo-tool\ja-netfilter.jar=jetbrains




